-- Minikube commands
minikube start --addons=ingress,ingress-dns --cpus=4 -p kms-test
helm -n kms upgrade --install --create-namespace kms ./KMS/ -f ./kms_helm_values.yaml
kubectl apply -f job_service.yaml -n kms
kubectl create serviceaccount index-api --namespace default

-- Deleting index-service
kubectl delete deployment index-service -n kms
kubectl delete service index-service -n kms

-- Only for running index-service on specific server.
kubectl -n kms port-forward service/index-service 8080:8080

-- All .yaml files
1. kms_helm_values.yaml = Helm values/credentials regarding kaggle, github api token etc
2. ui-indexer.yaml = Deploying the ui
3. job_service.yaml = Deploying the job service